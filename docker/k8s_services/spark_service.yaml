apiVersion: v1
kind: Service
metadata:
  name: spark-master
  namespace: de-dv
spec:
  selector:
    app: spark-master
  ports:
    - name: spark
      port: 7077
      targetPort: 7077
    - name: ui
      port: 8088
      targetPort: 8080
  type: ClusterIP

