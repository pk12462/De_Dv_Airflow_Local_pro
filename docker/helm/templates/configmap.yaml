apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "de-dv-platform.fullname" . }}-pipeline-config
  labels:
    {{- include "de-dv-platform.labels" . | nindent 4 }}
data:
  app_connection_config.yaml: |
{{ .Files.Get "../../pipelines/configs/app_connection_config.yaml" | indent 4 }}
  egress_rules.yaml: |
{{ .Files.Get "../../pipelines/egress/egress_rules.yaml" | indent 4 }}
  shield_values.yaml: |
{{ .Files.Get "../../pipelines/shield/shield_values.yaml" | indent 4 }}

